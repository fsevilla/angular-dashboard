import { UserStatusDirective } from './user-status.directive';

describe('UserStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new UserStatusDirective();
    expect(directive).toBeTruthy();
  });
});
